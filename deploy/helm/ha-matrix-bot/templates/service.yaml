apiVersion: v1
kind: Service
metadata:
  name: {{ include "ha-matrix-bot.fullname" . }}
  labels:
    {{- include "ha-matrix-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ha-matrix-bot.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
